# 手写识别功能入口说明

## 📍 功能位置

### 主要入口：图片全屏查看页面

**路径**：题目详情 → 图片全屏查看 → 手写输入按钮

**具体步骤**：
1. 打开应用，进入题目详情页面
2. 点击图片进入全屏查看模式（`ImageFullscreenWithNotesActivity`）
3. 在页面底部找到 **手写输入按钮**（`btnHandwritingInput`）
4. 点击按钮打开全屏手写识别界面（`ScreenHandwritingActivity`）

## 🎯 功能说明

### 全屏手写识别界面（ScreenHandwritingActivity）

**功能特点**：
- ✅ **边写边识别**：抬笔后自动识别（默认开启）
- ✅ **字体美化**：识别后可以转换为不同字体（常规、粗体、优雅、书法）
- ✅ **实时显示**：识别结果实时显示在上方文本框
- ✅ **连续书写**：可以连续书写多个字/词，结果会累积显示

**界面布局**：
```
┌─────────────────────────┐
│  返回按钮  |  工具栏      │
├─────────────────────────┤
│                         │
│    手写输入区域          │
│    (白色背景)            │
│                         │
├─────────────────────────┤
│  字体选择: [常规][粗体]   │
│         [优雅][书法]     │
├─────────────────────────┤
│  识别结果文本框          │
│  (显示识别后的文字)       │
├─────────────────────────┤
│ [识别文字] [清除] [确认] │
│  边写边识别: [开关]      │
└─────────────────────────┘
```

## 🔍 如何找到入口

### 方法 1：通过图片查看页面（推荐）

1. **打开应用**
2. **进入题目详情**：点击任意题目
3. **查看图片**：点击题目图片进入全屏查看
4. **找到手写按钮**：在页面底部工具栏中，找到手写输入图标按钮
5. **点击进入**：点击后打开全屏手写识别界面

### 方法 2：代码中查找

在 `ImageFullscreenWithNotesActivity.kt` 中：
```kotlin
// 手写输入按钮（打开全屏手写笔记）
val btnHandwritingInput: android.widget.ImageButton = findViewById(R.id.btnHandwritingInput)
btnHandwritingInput.setOnClickListener {
    ScreenHandwritingActivity.start(this) { recognizedText ->
        // 识别结果直接添加到输入框
        addNoteEditText.append(recognizedText)
    }
}
```

## 📱 使用流程

### 1. 打开手写识别界面
- 从图片全屏查看页面点击手写输入按钮

### 2. 开始书写
- 在白色手写区域用手指或手写笔书写
- 支持连续书写多个字/词

### 3. 自动识别（边写边识别模式）
- 写完一个字后，抬笔等待约 0.8 秒
- 系统自动识别并显示结果
- 已识别的内容会被清除，可以继续书写

### 4. 手动识别
- 如果边写边识别关闭，可以点击"识别文字"按钮手动识别

### 5. 选择字体
- 识别后，可以选择不同的字体样式
- 支持：常规、粗体、优雅、书法

### 6. 确认使用
- 点击"确认"按钮，识别结果会返回到笔记输入框

## 🎨 界面元素说明

### 工具栏按钮
- **返回按钮**：返回上一页面
- **识别文字**：手动触发识别
- **清除**：清除所有手写内容和识别结果
- **确认**：确认使用识别结果，返回到笔记输入框

### 字体选择器
- **常规**：默认字体
- **粗体**：加粗字体
- **优雅**：细体字体
- **书法**：书法风格字体

### 边写边识别开关
- **开启**：抬笔后自动识别（推荐）
- **关闭**：需要手动点击"识别文字"按钮

## 💡 使用技巧

1. **书写清晰**：尽量写清楚，避免潦草
2. **笔画完整**：确保每个字都完整写完
3. **适当间距**：字与字之间保持适当距离
4. **利用边写边识别**：开启后可以连续书写，自动识别
5. **查看识别结果**：如果第一个结果不准确，可以查看日志中的其他候选结果

## 🔧 如果找不到入口

### 检查项：
1. **确认应用版本**：确保是最新版本
2. **检查布局文件**：确认 `activity_image_fullscreen_with_notes.xml` 中有 `btnHandwritingInput` 按钮
3. **查看日志**：如果按钮存在但不显示，检查是否有布局错误

### 临时测试方法：
可以在代码中直接调用：
```kotlin
ScreenHandwritingActivity.start(this) { recognizedText ->
    // 处理识别结果
    Log.d("Test", "识别结果: $recognizedText")
}
```

## 📝 相关文件

- `ScreenHandwritingActivity.kt` - 全屏手写识别界面
- `HandwritingInputView.kt` - 手写输入视图组件
- `ImageFullscreenWithNotesActivity.kt` - 图片全屏查看页面（包含入口）
- `activity_screen_handwriting.xml` - 手写识别界面布局
- `activity_image_fullscreen_with_notes.xml` - 图片全屏查看布局


# PaddleOCR 模型文件目录

## 📋 说明

此目录用于存放PaddleOCR的模型文件。

## 📁 目录结构

```
app/src/main/assets/models/
├── det/                    # 文本检测模型
│   ├── inference.pdmodel
│   └── inference.pdiparams
├── rec/                    # 文本识别模型
│   ├── inference.pdmodel
│   └── inference.pdiparams
├── cls/                    # 方向分类模型（可选）
│   ├── inference.pdmodel
│   └── inference.pdiparams
└── README.md
```

## 📥 模型文件来源

### 方式1：使用当前项目的模型文件

当前项目已有模型文件在：
```
app/src/main/assets/paddleocr/
├── ch_PP-OCRv4_det_infer/
├── ch_PP-OCRv4_rec_infer/
└── ch_ppocr_mobile_v2.0_cls_infer/
```

可以复制或链接到 `models/` 目录。

### 方式2：从官方Demo复制

从PaddleOCR官方Android Demo复制模型文件：
```
android_demo/app/src/main/assets/models/
```

### 方式3：从PaddleOCR官方下载

下载地址：
- https://github.com/PaddlePaddle/PaddleOCR
- 模型下载页面会提供模型文件

## ⚠️ 注意事项

1. **模型格式**：
   - 官方Demo可能使用 `.nb` 格式（PaddleLite格式）
   - 当前项目使用 `.pdmodel` 和 `.pdiparams` 格式
   - 可能需要转换格式或使用Demo中的模型

2. **文件大小**：
   - 检测模型：约5-10MB
   - 识别模型：约10-15MB
   - 分类模型：约1-2MB
   - 总计：约15-30MB

3. **模型版本**：
   - 确保模型版本与.so库版本兼容

